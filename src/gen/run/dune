(tests
 (names normalize_idempotent render_roundtrip no_trailing_blank_in_blocks
   struct_props)
 (libraries cmarkit_ cmarkit_generator qcheck-core.runner fmt)
 (preprocess
  (pps ppx_jane ppx_expect)))
